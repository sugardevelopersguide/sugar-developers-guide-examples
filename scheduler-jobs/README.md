# Scheduler Jobs

Examples for defining and registering scheduled Sugar jobs.

The `custom/` directory mirrors Sugar's deployment paths.

## Build a Module Loadable Package

```sh
php bin/pack.php scheduler-jobs 26.7.0
```

The archive is written to `scheduler-jobs/builds/`.
