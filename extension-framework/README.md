# Extension Framework

Examples for creating upgrade-safe Sugar customizations through the Extension
Framework.

- `custom/` contains source files in their Sugar deployment paths.
- `package/` contains installable package examples.
