# Logic Hooks

Examples for responding to Sugar bean and application lifecycle events.

The `custom/` directory mirrors Sugar's deployment paths.

## Build a Module Loadable Package

```sh
php bin/pack.php logic-hooks 26.7.0
```

The archive is written to `logic-hooks/builds/`.
